{"id":1456541346,"question_id":817,"lang":"cpp","lang_name":"C++","time":"2 months, 2 weeks","timestamp":1731957643,"status":10,"status_display":"Accepted","runtime":"36 ms","url":"/submissions/detail/1456541346/","is_pending":"Not Pending","title":"Design HashMap","memory":"67.5 MB","code":"class MyHashMap {\n    vector<vector<pair<int,int>>> mp;\n    const int size = 10000;\npublic:\n    MyHashMap() {\n        mp.resize(size);\n        \n    }\n    \n    void put(int key, int value) {\n        int index = key % size;\n        vector<pair<int,int>>& row = mp[index];\n        for(auto iter = row.begin(); iter!=row.end(); iter++) {\n            if(iter->first == key) {\n                iter->second = value;\n                return;\n            }\n        }\n        row.push_back({key, value});\n    }\n    \n    int get(int key) {\n        int index = key % size;\n        vector<pair<int,int>>& row = mp[index];\n        for(auto iter = row.begin(); iter!=row.end(); iter++) {\n            if(iter->first == key)              \n                return iter->second;\n        }\n        return -1;\n        \n    }\n    \n    void remove(int key) {\n       int index = key % size;\n        vector<pair<int,int>>& row = mp[index];\n        for(auto iter = row.begin(); iter!=row.end(); iter++) {\n            if(iter->first == key) {              \n                row.erase(iter);\n                return;\n            }\n        }       \n    }\n};\n\n/**\n * Your MyHashMap object will be instantiated and called as such:\n * MyHashMap* obj = new MyHashMap();\n * obj->put(key,value);\n * int param_2 = obj->get(key);\n * obj->remove(key);\n */","compare_result":"1111111111111111111111111111111111111","title_slug":"design-hashmap","has_notes":false,"flag_type":1}